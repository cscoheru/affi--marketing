[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile.railway"

[deploy]
startCommand = "./server"
healthcheckPath = "/health"
healthcheckTimeout = 300

[[services]]
name = "affi-marketing-api"
